# Scan-Host
A tool for scanning domains and IPs. It checks ping, HTTP status, IP info, server location, and more. It also detects whether a website’s traffic is Iranian or international, and shows if the traffic is half-price (Iran-only feature).
